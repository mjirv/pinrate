<%= form_for([@pin, @pin.ratings.build]) do |f| %>
	<p>
		<%= f.label :rating %><br>
		<%= f.range_field(:rating, in: 1..5, step: 1) %><br />
	</p>
	<p>
		<%= f.label :comment %><br>
		<%= f.text_area :comment%>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>